// Function in JS can be declared in two ways

// 1. function declaration
// fully hoisted
function foo(params) {
  //
}

// 2. function expression
// not hoisted untill initialization

let foo = function (params) {
  //
};
